interface:
  display_name: "Status Report"
  short_description: "Summarize branches and worktrees"
  default_prompt: "Use $status-report to summarize local branches and worktrees."
policy:
  allow_implicit_invocation: true
