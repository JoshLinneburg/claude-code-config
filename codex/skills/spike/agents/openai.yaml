interface:
  display_name: "Spike"
  short_description: "Run a structured investigation"
  default_prompt: "Use $spike to investigate the technical question and write up options."
policy:
  allow_implicit_invocation: true
